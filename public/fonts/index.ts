// place in this folder your custom fonts
// if posible use Google Fonts or Typekit since Next.js already does Font Optimization
// and adds them as inline font CSS at build time, so it's server side rendered
// https://nextjs.org/docs/basic-features/font-optimization

export {};
