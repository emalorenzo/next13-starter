import HomePage from './HomePage';

export default async function Page() {
  // If you need to fetch data here is the place, since this is a Server Component
  // Forward fetched data to your Client Component
  return <HomePage />;
}
